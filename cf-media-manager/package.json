{
    "name": "cf-media-manager",
    "version": "3.0.0",
    "description": "CF Media Manager release wrapper.",
    "private": true,
    "scripts": {
        "build": "node ../scripts/release.mjs cf-media-manager",
        "clean": "rm -rf dist/*.zip"
    }
}
